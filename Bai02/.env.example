DB_HOST=localhost
DB_PORT=3306
DB_NAME=partition_demo
DB_USER=root
DB_PASSWORD=root
# mysql (mặc định, mysql2) — tương thích MariaDB/MySQL trên máy
# mariadb — dùng gói mariadb (Docker/Linux thường ổn)
DB_DIALECT=mysql
PORT=3000
# DB_LOGGING=1
